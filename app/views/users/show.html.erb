<div class="container" align="center">
  <br><br>
<h3 id="notice"><%= notice %></h3>
<br><br>
<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>

<p>
  <strong>Name:</strong>
  <%= @user.name %>
</p>

<!p>
  <!strong><!/strong>
  <!%= @user.password %>
<!/p>

<!p>
  <!strong><!/strong>
  <!%= @user.is_admin %>
<!/p>

<!%= link_to 'Edit', edit_user_path(@user) %>
<% if isadmin%>
      <button type="button" class="btn"> <%= link_to 'Back', users_path %></button>
<% else %>
      <button type="button" class="btn"> <%= link_to 'sign in', home_index_path %></button>

<%end%>
    </div>