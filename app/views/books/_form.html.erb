<div class="container" align="center">
<%= form_for(@book) do |f| %>
  <% if @book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@book.errors.count, "error") %> prohibited this book from being saved:</h2>

      <ul>
      <% @book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :isbn %><br>
    <%= f.text_field :isbn, :class=>"textbox1"%>
  </div>
  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title, :class=>"textbox1"%>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description, :class=>"textbox1"%>
  </div>
  <div class="field">
    <%= f.label :author %><br>
    <%= f.text_field :author, :class=>"textbox1"%>
  </div>

      <%= f.submit "Create", class: "btn" %>

      <button type="button" class="btn"> <%= link_to 'Back', books_path %></button>
      </div>

<% end %>
