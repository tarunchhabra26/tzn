<p id="notice"><%= notice %></p>

<h1>Listing Checks</h1>

<table>
  <thead>
    <tr>
      <th>Id</th>
      <th>Isbn</th>
      <th>Email</th>
      <th>Checkout</th>
      <th>Checkin</th>
      <th>Status</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @histories.each do |history| %>
      <tr>
        <td><%= history.id %></td>
        <td><%= history.isbn %></td>
        <td><%= history.email %></td>
        <td><%= history.checkout %></td>
        <td><%= history.checkin %></td>
        <td><%= history.status %></td>
        <td><%= link_to 'Show', check %></td>
        <td><%= link_to 'Edit', edit_check_path(check) %></td>
        <td><%= link_to 'Destroy', check, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New History', new_check_path %>
